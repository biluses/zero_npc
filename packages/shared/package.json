{
  "name": "@zero-npc/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain constants, types and validators between web and api",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "lint": "echo 'No lint yet' && exit 0",
    "typecheck": "echo 'No typecheck yet' && exit 0",
    "test": "echo 'No tests yet' && exit 0",
    "build": "echo 'No build step' && exit 0",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
